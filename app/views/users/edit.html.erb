<% provide(:title, "Edit profile") %>
<h1>Edit Profile</h1>

<div class="eight columns offset-by-four">
	<%= form_for @user do |f| %>

		<%= render 'fields', :f => f %>
		<%= f.submit "Save updates", :class => "btn" %>

	<% end %>
</div>